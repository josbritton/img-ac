name: ci

on:
  push:

jobs:
  packer:
    runs-on: ubuntu-latest
    name: Run Packer
    steps:
      - name: Checkout
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98

      - name: Setup packer
        uses: hashicorp/setup-packer@55fb36cb9099bde5cf659dfff2493ddb607db9c8
        id: setup
        with:
          version: latest

      - name: Run `packer init`
        id: init
        run: "packer init ."

      - name: Run `packer fmt`
        id: format
        run: "packer fmt --recursive --check --diff --write=false ."

      - name: Run `packer validate` (syntax)
        id: validate
        run: "packer validate --no-warn-undeclared-var --syntax-only ."
